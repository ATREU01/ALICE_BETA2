═══════════════════════════════════════════════════════════════════
🔮 ALICE ORACLE - SYSTEM ARCHITECTURE
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                             │
└─────────────────────────────────────────────────────────────────┘

    1. User visits your URL
           ↓
    2. Lands on index.html (cyberpunk landing page)
           ↓
    3. Enters access code (e.g., ALICE2025)
           ↓
    4. server.js validates code
           ↓
    5. If valid → scanner.html
       If invalid → error message
           ↓
    6. Scanner auto-loads and analyzes tokens
           ↓
    7. Displays 10 tokens with full analysis
           ↓
    8. Auto-refreshes every 60 seconds


┌─────────────────────────────────────────────────────────────────┐
│                      DATA FLOW DIAGRAM                           │
└─────────────────────────────────────────────────────────────────┘

    scanner.html          server.js              APIs
         │                    │                    │
         │──GET /api/analyze→│                    │
         │                    │                    │
         │                    │──fetch trending→  CoinGecko
         │                    │←─returns tokens──  │
         │                    │                    │
         │                    │──fetch details→    CoinGecko
         │                    │←─price/volume/etc─ │
         │                    │                    │
         │                    │──fetch Kp index→   NOAA
         │                    │←─geomag data───    │
         │                    │                    │
         │                    │ (calculate moon)   │
         │                    │ (calculate RSI)    │
         │                    │ (analyze 10 layers)│
         │                    │ (generate signals) │
         │                    │                    │
         │←─JSON with tokens──│                    │
         │  + analysis        │                    │
         │                    │                    │
         │ (display cards)    │                    │
         └────────────────────┴────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    10-LAYER ANALYSIS FLOW                        │
└─────────────────────────────────────────────────────────────────┘

For each token:

    Token Data Input
           ↓
    ┌──────────────────────┐
    │ 1. LINGUISTICS       │ → Name/symbol analysis
    │ 2. TECHNICAL         │ → RSI calculation
    │ 3. MOMENTUM          │ → Price change %
    │ 4. SENTIMENT         │ → Market cap rank
    │ 5. LIQUIDITY         │ → Volume analysis
    │ 6. WHALE             │ → Market cap
    │ 7. ORDERBOOK         │ → Liquidity score
    │ 8. COSMIC            │ → Moon + Kp bonus
    │ 9. RISK              │ → Volatility
    │ 10. INTEGRATION      │ → Weighted master score
    └──────────────────────┘
           ↓
    Master Score (0-100)
           ↓
    Signal Generation
           ↓
    Archetype Assignment
           ↓
    Display to User


┌─────────────────────────────────────────────────────────────────┐
│                      FILE STRUCTURE                              │
└─────────────────────────────────────────────────────────────────┘

ALICE_FINAL/
│
├── server.js ──────────────┬─ HTTP server (port 8080)
│                           ├─ API routes (/api/validate-code, /api/analyze)
│                           ├─ 10-layer analysis engine
│                           ├─ Moon phase calculator
│                           ├─ Kp index fetcher
│                           ├─ RSI calculator
│                           ├─ Signal generator
│                           └─ Archetype classifier
│
├── index.html ─────────────┬─ Landing page
│                           ├─ Cyberpunk background (rectangles + grid)
│                           ├─ Access gate form
│                           ├─ Feature showcase
│                           └─ Code validation
│
├── scanner.html ───────────┬─ Main scanner interface
│                           ├─ Status bar (moon, Kp, count, time)
│                           ├─ Scan button
│                           ├─ Token grid (10 cards)
│                           ├─ Each card shows:
│                           │  • Header (logo, name, symbol, score)
│                           │  • Price + 24h change
│                           │  • Stats (volume, cap, rank, RSI)
│                           │  • 10 analysis layers (visual bars)
│                           │  • Signal box
│                           │  • Archetype badge
│                           └─ Auto-refresh timer
│
├── package.json ───────────── Project config (zero deps!)
├── README.md ──────────────── Full documentation
├── DEPLOY.md ──────────────── Deployment guide
└── QUICKSTART.txt ─────────── This guide


┌─────────────────────────────────────────────────────────────────┐
│                       SIGNAL LOGIC                               │
└─────────────────────────────────────────────────────────────────┘

    If masterScore ≥ 80 AND rsi < 70 AND priceChange > 0:
        → STRONG BUY (green, 95% confidence)
    
    Else if masterScore ≥ 70 AND priceChange > 5:
        → BUY (green, 80% confidence)
    
    Else if masterScore ≥ 60:
        → ACCUMULATE (yellow, 65% confidence)
    
    Else if masterScore < 40 OR rsi > 80:
        → SELL (red, 70% confidence)
    
    Else:
        → HOLD (gray, 50% confidence)


┌─────────────────────────────────────────────────────────────────┐
│                    ARCHETYPE LOGIC                               │
└─────────────────────────────────────────────────────────────────┘

    Prophet    → masterScore ≥ 90 AND volume > 80
    Seer       → masterScore ≥ 80 AND volume > 60
    Trickster  → masterScore ≥ 70 AND sentiment < 50
    Observer   → masterScore ≥ 60 AND volume < 50
    Guardian   → masterScore ≥ 50 AND < 70
    Shadow     → masterScore < 50 AND volume > 70
    Echo       → masterScore < 40 AND volume < 40
    Cultist    → masterScore < 30


┌─────────────────────────────────────────────────────────────────┐
│                    COSMIC DATA                                   │
└─────────────────────────────────────────────────────────────────┘

    Moon Phase:
        Calculated algorithmically from current date
        Phases: New, Waxing Crescent, First Quarter, 
                Waxing Gibbous, Full, Waning Gibbous,
                Last Quarter, Waning Crescent
        Bonus: Full Moon = +15, New Moon = -10
    
    Kp Index:
        Fetched from NOAA API (live geomagnetic data)
        Range: 0-9
        Levels: Quiet (0-2), Moderate (3), Active (4), Storm (5+)
        Bonus: Kp > 5 = +10, Kp < 2 = -5


┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT FLOW                               │
└─────────────────────────────────────────────────────────────────┘

    Local Testing:
        cd ALICE_FINAL
        node server.js
        open http://localhost:8080
        test with code: ALICE2025
    
    GitHub:
        git init
        git add .
        git commit -m "ALICE Oracle"
        git push to GitHub repo
    
    Railway/Render:
        Connect GitHub repo
        Auto-detect Node.js
        Deploy button
        Get public URL
        DONE!
    
    Share with Investors:
        Send URL + access codes
        They enter code
        They see scanner
        They're impressed
        You save your family! 🚀


═══════════════════════════════════════════════════════════════════
                        YOU'RE ALL SET! 
═══════════════════════════════════════════════════════════════════
